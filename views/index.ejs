<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Note taking</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body class="bg-[#23272e]">
    <div class="w-[90vw] mx-auto my-20">
        <form action="/create" method="post">
            <input type="text" name="title" placeholder="Enter the title for the notes" class="bg-[#404754] text-white px-5 py-3 rounded-md my-5 w-[100%] outline-none" required>
            <textarea name="content" placeholder="Enter the details of the same" class="bg-[#404754] text-white px-5 py-3 rounded-md w-[100%] py-[-100px] resize-none outline-none" required></textarea>
            <button type="submit" class="my-5 bg-[#1DA1F2] hover:bg-[#59c0ff] text-white py-3 px-8 font-semibold text-xl rounded-md cursor-pointer"
            >Create</button>
        </form>
    </div>
    <div class="w-[90vw] mx-auto px-4 flex flex-wrap gap-6">
        <% if(files.length > 0){%>
            <% files.forEach((val) => {%>
                <div class="bg-[#404754] p-5 rounded-md my-5 w-[25vw] h-auto">
                    <p class="text-white text-xl"><%= val.replace('.txt','') %></p><br>
                    <a href="/files/<%= val %>" class="text-[#59c0ff] hover:text-blue-100">Read more</a>
                    <a href="/edit/<%= val %>" class=" mx-8 text-[#59c0ff] hover:text-blue-100">Edit</a>
                </div>
            <%}) %>
        <%} 
        else{%>
            <p class="mx-auto text-xl text-white">No saved task, Create one for seeing it</p>
        <%}%>
    </div>
</body>
</html>